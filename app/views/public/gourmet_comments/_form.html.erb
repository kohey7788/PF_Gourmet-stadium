<%= form_with(model: @gourmet_comment, url: gourmet_gourmet_comments_path(gourmet.id), remote: true) do |f| %>
  <%= f.text_area :comment, cols: '60',rows:'5',placeholder: "コメントをここに" %>
  <%= f.submit "送信する" %>
<% end %>